import ProfileList from "./ProfileList";

const Home = () => {
  return (
    <div class="w-full h-full flex flex-row">
      <div class="w-full mx-2">
        <ProfileList>
        </ProfileList>
      </div>
    </div>
  );
};

export default Home;
